/* ESTRUTURA INICIAL */

/* Assim é "declaração literal" */
/* Quando chama a função, o nome é "argumento" */
/* Quando declara uma função, o nome é "parâmetro" */
function nomeDaFuncao() {
  //Código aqui

  //No final retorna explicitamente ou implicitamente
}

let variavel = 'Esta string será substituida pelo return da próxima função'

function funcReturnUndefined() {
  let num1 = 10,
    num2 = 10,
    soma = num1 + num2
}

variavel = funcReturnUndefined()